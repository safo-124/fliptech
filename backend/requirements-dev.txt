# Fliiptech Skills Hub - backend development dependencies
# Install with:  pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

# --- Tests ---
pytest==9.1.1
pytest-django==4.14.0
pytest-cov==7.1.0
factory-boy==3.3.3
Faker==40.36.0

# --- Lint and format ---
# ruff only. `ruff format` supersedes black; installing both means two formatters
# disagreeing over the same files. The PDF's Section 06 lists both - drop black.
ruff==0.16.3

# --- Local inspection ---
django-debug-toolbar==7.1.1
