// Tailwind v4 moved the PostCSS plugin into its own package and dropped
// tailwind.config.js in favour of CSS-first configuration. See globals.css.
export default { plugins: { "@tailwindcss/postcss": {} } };
